require.config({
   paths:{
       "MKCanvas":"MKCanvas",
       "commentSpaceAllocator":"commentSpaceAllocator",
       "CommentBulider":"CommentBuilder",
       "CommentLoader":"CommentLoader",
       "run":"run"
   } 
});
require(["run"],function(){
    
});